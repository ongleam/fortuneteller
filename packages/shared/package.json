{
  "name": "@fortuneteller/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "공유 커널 — 타입·유틸·상수 (leaf 패키지, 다른 워크스페이스 패키지에 의존하지 않는다).",
  "exports": {
    "./utils": "./utils/index.ts",
    "./constants": "./constants/index.ts",
    "./*": "./*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test ."
  },
  "dependencies": {
    "ai": "6.0.208",
    "bcrypt-ts": "^5.0.2",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.8.6",
    "bun-types": "^1.3.11",
    "typescript": "^5.6.3"
  }
}
